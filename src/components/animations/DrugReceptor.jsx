import {useState} from 'react'; export default function DrugReceptor(){const [step,setStep]=useState(0);const steps=[['Drug','A drug approaches its biological target.'],['Receptor','Binding occurs at the receptor.'],['Response','The interaction produces or changes a biological response.']];return <div className="animation"><div className="anim-title">Interactive: Drug → Receptor → Response</div><div className="flow">{steps.map((s,i)=><div className={i===step?'node active':'node'} key={s[0]} onClick={()=>setStep(i)}><div className="node-icon">{i===0?'💊':i===1?'🎯':'⚡'}</div><b>{s[0]}</b>{i<2&&<span>→</span>}</div>)}</div><div className="anim-explain"><b>Step {step+1}: {steps[step][0]}</b><p>{steps[step][1]}</p></div><div className="controls"><button onClick={()=>setStep(0)}>Reset</button><button onClick={()=>setStep((step+1)%3)}>Next step</button></div></div>}
